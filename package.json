{
  "name": "alyok-autotag",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --outfile=main.js --target=es2020 --external:obsidian --format=cjs"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
